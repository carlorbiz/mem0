name = "voice-transcribe"
main = "transcribe-audio.js"
compatibility_date = "2024-01-01"

# Cloudflare Workers AI binding
# This enables the AI object in the worker environment
[ai]
binding = "AI"

# Optional: Configure custom routes or triggers
# [triggers]
# crons = []

# Development settings
[env.production]
name = "voice-transcribe"

# Workers AI is available on the free tier with limits:
# - 10,000 neurons per day (Whisper uses ~1 neuron per second of audio)
# - Rate limits apply
# Pricing after free tier: ~$0.011 per 1,000 neurons
